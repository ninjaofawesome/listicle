var $ = require('jquery');

module.export = function() {
  console.log('images js loaded');
  // todo:  Hook this up, then select multiple dom elements to manipulate on large or small dom elements.

}